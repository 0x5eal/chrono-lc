[package]
name = "chrono-locale"
version = "0.1.0"
authors = ["Alessandro Pellizzari <alex@amiran.it>"]
# build = "build.rs"

[dependencies]
chrono = "0.4"
num-integer = { version = "0.1", default-features = false }
lazy_static = "1.2"

[build-dependencies]
serde = "1"
serde_json = "1"
serde_derive = "1"
walkdir = "2"
